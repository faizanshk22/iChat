<%= form_with(model: @chatroom, remote: true, class: "d-flex" ) do |f| %>
  <%= f.text_field :name, class: "form-control", autocomplete: 'off', placeholder: 'Enter Room' %>
  <%= f.submit data: { disable_with: false } %>
<% end %>